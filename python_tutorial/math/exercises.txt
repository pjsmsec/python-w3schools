Consider the following code:
    print(max(5, 10, 25))
What will be the printed result?

    25

Consider the following code:
    print(pow(2, 3))
What will be the printed result?

    8

When using the built-in math module, what will be the printed result of the 
following code:
    import math
    print(math.sqrt(9))

    3

When using the built-in math module, how can you return the number of PI?

    math.pi
