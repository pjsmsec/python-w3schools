Which character can be used as a default case value in a match statement?
    _

Insert the correct keywords to make a match statement:
    day = 4
    match day:
        case 6:
            print('Saturday')
        case 7:
            print('Sunday')
        case _:
            print('Looking forward to the weekend')

Which operator is used as an OR operator in case evaluation?
    |
